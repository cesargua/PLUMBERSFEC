version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm i
    build:
      commands:
        - npm run server-dev
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
     preBuild:
      commands:
        - npm i
    build:
      commands: 
        - npm run client-dev
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
